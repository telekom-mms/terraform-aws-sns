
---
name: terraform-docs

on:
  push:
    branches: [main]

env:
  TEMPLATE_REPOSITORY: telekom-mms/terraform-template

jobs:
  call-terraform-docs:
    if: github.repository != '$TEMPLATE_REPOSITORY'
    uses: telekom-mms/.github/.github/workflows/terraform-docs.yml@eda1dcd90f2ea8506e5a882e37761d2dd07a6adf # main
